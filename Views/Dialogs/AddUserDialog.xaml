<Window x:Class="GymManagementSystem.Views.Dialogs.AddUserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Add User" 
        Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    <StackPanel Margin="30">
        <TextBlock Text="Add New User" FontSize="24" FontWeight="Bold" Margin="0,0,0,30"/>

        <TextBox x:Name="txtUsername" 
                 materialDesign:HintAssist.Hint="Username*"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,15"/>

        <PasswordBox x:Name="txtPassword" 
                     materialDesign:HintAssist.Hint="Password*"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                     Margin="0,0,0,15"/>

        <PasswordBox x:Name="txtConfirmPassword" 
                     materialDesign:HintAssist.Hint="Confirm Password*"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                     Margin="0,0,0,15"/>

        <TextBox x:Name="txtFullName" 
                 materialDesign:HintAssist.Hint="Full Name*"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Margin="0,0,0,15"/>

        <ComboBox x:Name="cmbRole" 
                  materialDesign:HintAssist.Hint="Role*"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}"
                  Margin="0,0,0,30">
            <ComboBoxItem Content="Admin"/>
            <ComboBoxItem Content="Staff" IsSelected="True"/>
        </ComboBox>

        <!-- Action Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="CANCEL" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Width="100" Height="40" Margin="0,0,10,0"
                    Click="btnCancel_Click"/>
            <Button x:Name="btnSave" Content="CREATE USER" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Width="130" Height="40"
                    Click="btnSave_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
