<Window x:Class="GymManagementSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Gym Management System - Login" 
        Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{DynamicResource PrimaryHueMidBrush}" Padding="20">
            <StackPanel HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="Dumbbell" Width="60" Height="60" 
                                         Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="GYM MANAGEMENT" FontSize="24" FontWeight="Bold" 
                           Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <TextBlock Text="SYSTEM" FontSize="18" 
                           Foreground="White" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" Margin="40" VerticalAlignment="Center">
            <TextBlock Text="Sign In" FontSize="22" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBox x:Name="txtUsername" 
                     materialDesign:HintAssist.Hint="Username"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20"
                     FontSize="14"/>

            <PasswordBox x:Name="txtPassword" 
                         materialDesign:HintAssist.Hint="Password"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,0,0,10"
                         FontSize="14"/>

            <TextBlock x:Name="txtError" 
                       Text="" 
                       Foreground="Red" 
                       FontSize="12" 
                       Margin="0,0,0,20"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>

            <Button x:Name="btnLogin" 
                    Content="LOGIN" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Height="45"
                    FontSize="14"
                    FontWeight="Bold"
                    Click="btnLogin_Click"/>

            <TextBlock Text="Default credentials: admin / admin123" 
                       FontSize="11" 
                       Foreground="Gray" 
                       HorizontalAlignment="Center" 
                       Margin="0,20,0,0"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
