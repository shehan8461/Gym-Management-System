<Page x:Class="GymManagementSystem.Views.Pages.AttendancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GymManagementSystem.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="Attendance Log">

    <Grid Background="#F5F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                <TextBlock Text="Attendance Logs" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center" Margin="0,0,20,0"/>
                
                <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="dpStartDate" Width="120" Margin="0,0,15,0"/>
                
                <TextBlock Text="To:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <DatePicker x:Name="dpEndDate" Width="120" Margin="0,0,15,0"/>
                
                <Button x:Name="btnLoad" Content="LOAD LOGS" Padding="16,0" Height="36" 
                        Background="#34495E" Foreground="White" BorderThickness="0" 
                        Click="btnLoad_Click" Cursor="Hand" FontWeight="SemiBold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- Content -->
        <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="8" Effect="{DynamicResource DataBlockShadow}" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock x:Name="txtStatus" Text="Select dates and click Load." Foreground="Gray" Margin="0,0,0,10"/>
                
                <DataGrid x:Name="dgAttendance" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" 
                          IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          Background="White" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date &amp; Time" Binding="{Binding DateTimeString}" Width="200"/>
                        <DataGridTextColumn Header="Member Name" Binding="{Binding MemberName}" Width="*"/>
                        <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeNo}" Width="150"/>
                        <DataGridTextColumn Header="Event Type" Binding="{Binding EventTypeString}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
                
                <ProgressBar x:Name="progressBar" Grid.Row="1" IsIndeterminate="True" 
                             Width="200" Height="4" VerticalAlignment="Top" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</Page>
