<Page x:Class="GymManagementSystem.Views.Pages.AttendancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Attendance">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Attendance Management" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="btnMarkAttendance" 
                    Content="MARK ATTENDANCE" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="20,0,0,0"
                    Height="40"
                    Click="btnMarkAttendance_Click"/>
        </StackPanel>

        <!-- Filter Options -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtSearch" 
                     Grid.Column="0"
                     materialDesign:HintAssist.Hint="Search by member name"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     FontSize="14"
                     TextChanged="txtSearch_TextChanged"/>

            <DatePicker x:Name="dpFilterDate" 
                        Grid.Column="1"
                        materialDesign:HintAssist.Hint="Filter by Date"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                        Width="150"
                        Margin="10,0,0,0"
                        SelectedDateChanged="dpFilterDate_SelectedDateChanged"/>

            <Button x:Name="btnToday" 
                    Grid.Column="2"
                    Content="TODAY" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="10,0,0,0"
                    Height="40"
                    Click="btnToday_Click"/>

            <Button x:Name="btnRefresh" 
                    Grid.Column="3"
                    Content="REFRESH" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="10,0,0,0"
                    Height="40"
                    Click="btnRefresh_Click"/>
        </Grid>

        <!-- Attendance DataGrid -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid x:Name="dgAttendance" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding AttendanceId}" Width="60"/>
                    <DataGridTextColumn Header="Member Name" Binding="{Binding MemberName}" Width="*"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding CheckInDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Check-In Time" Binding="{Binding CheckInTimeDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Check-Out Time" Binding="{Binding CheckOutTimeDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding AttendanceType}" Width="100"/>
                    <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</Page>
