<Page x:Class="GymManagementSystem.Views.Pages.PaymentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Payments">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Payment Management" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Filter Options -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtSearch" 
                     Grid.Column="0"
                     materialDesign:HintAssist.Hint="Search by member name"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     FontSize="14"
                     TextChanged="txtSearch_TextChanged"/>

            <ComboBox x:Name="cmbStatusFilter" 
                      Grid.Column="1"
                      materialDesign:HintAssist.Hint="Filter by Status"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      Width="150"
                      Margin="10,0,0,0"
                      SelectionChanged="cmbStatusFilter_SelectionChanged">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="Paid"/>
                <ComboBoxItem Content="Due Soon"/>
                <ComboBoxItem Content="Overdue"/>
            </ComboBox>

            <Button x:Name="btnRefresh" 
                    Grid.Column="2"
                    Content="REFRESH" 
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="10,0,0,0"
                    Height="40"
                    Click="btnRefresh_Click"/>
        </Grid>

        <!-- Payments DataGrid -->
        <materialDesign:Card Grid.Row="2">
            <DataGrid x:Name="dgPayments" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding PaymentId}" Width="60"/>
                    <DataGridTextColumn Header="Member Name" Binding="{Binding MemberName}" Width="*"/>
                    <DataGridTextColumn Header="Package" Binding="{Binding PackageName}" Width="150"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=LKR {0:N2}}" Width="120"/>
                    <DataGridTextColumn Header="Payment Date" Binding="{Binding PaymentDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Next Due" Binding="{Binding NextDueDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="120"/>
                    <DataGridTextColumn Header="Method" Binding="{Binding PaymentMethod}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </materialDesign:Card>
    </Grid>
</Page>
